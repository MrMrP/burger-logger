<h1>Come and Get UR Burgers!!</h1>

<h2>Newly Available!</h2>

<ul>
  {{#each this.burgers}}
    {{#unless devoured}}
      <li>
      {{this.burger_name}}
        </button>
        <button class="delete-burger" data-id="{{this.id}}">Devour IT!  
        </button>
      </li>
    {{/unless}}
  {{/each}}
</ul>

<h2>Place an Order</h2>
<ul>
  {{#each this.burgers}}
    {{#if devoured}}
    {{/if}}
  {{/each}}
</ul>

<form class="create-form">
      <div class="form-group">
        <label for="ca">Burger Name</label>
        <input type="text" id="name" name="burger_name">
      </div>
      <button type="submit">Add Burger</button>
</form>
